# Path to mediawiki-docker-dev, absolute or relative to the location of this file.
# Try to use the value of $DOCKER_MW_PATH if set.
# That's the variable used by mediawiki-docker-dev itself.
MWDD_DIR="$DOCKER_MW_PATH"

# Path to mediawiki, absolute or relative to the location of this file.
# Try to use the value of $MW_INSTALL_PATH if set.
# That's the variable used by mediawiki, which is also set by quibble for CI.
MW_DIR="$MW_INSTALL_PATH"
